apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "com.github.ohbaron.dropwizardkotlinexamplev1-v1.fullname" . }}-test-connection"
  labels:
    {{- include "com.github.ohbaron.dropwizardkotlinexamplev1-v1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "com.github.ohbaron.dropwizardkotlinexamplev1-v1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
